export const btnReset = `
    button,
    input[type="button"] {
        border: none;
        border-radius: 0;
        background: transparent;
        color: #fff;
        cursor: pointer;
        display: inline-block;
        padding: 1em 1.5em;
        text-transform: uppercase;
    }
`;
